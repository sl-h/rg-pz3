<Window x:Class="IveGrid3D.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helix="http://helix-toolkit.org/wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <helix:HelixViewport3D ClipToBounds="True" MouseDown="OnLeftMouseButtonDown" Name="Viewport"
                               MouseLeftButtonUp="OnLeftMouseButtonUp" MouseMove="Pan"
                               MouseWheel="MouseWheal" ShowCameraTarget="False">
            <helix:SunLight/>

        </helix:HelixViewport3D>
    </Grid>
</Window>
